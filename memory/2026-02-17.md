
## 2026-02-17

- **Setup**: Connected Telegram channel.
- **Memory**: Enabled QMD with Ollama embeddings ().
- **Models**: Configured tiered strategy.
  - Primary: `ollama/kimi-k2.5:cloud`
  - Fallback 1: `google/gemini-3-pro-preview`
  - Fallback 2: `ollama/minimax-m2.5:cloud`
- **Automation**: 
  - Created `health-check.sh` for model auto-switching (runs every 5m via crontab).
  - Set up daily GitHub backup to private repo `oguzhandilber/openclaw-backup` (runs at 02:00 TRT via crontab).
- **Identity**: Confirmed user is Oguzhan, timezone Europe/Istanbul.

## 2026-02-17

- **Setup**: Connected Telegram channel.
- **Memory**: Enabled QMD with Ollama embeddings (nomic-embed-text).
- **Models**: Configured tiered strategy.
  - Primary: ollama/kimi-k2.5:cloud
  - Fallback 1: google/gemini-3-pro-preview
  - Fallback 2: ollama/minimax-m2.5:cloud
- **Automation**: 
  - Created health-check.sh for model auto-switching (runs every 5m via crontab).
  - Set up daily GitHub backup to private repo oguzhandilber/openclaw-backup (runs at 02:00 TRT via crontab).
- **Identity**: Confirmed user is Oguzhan, timezone Europe/Istanbul.

- **Automation Update**: Enhanced `health-check.sh` to trigger `openclaw gateway restart` upon model switching. This ensures configuration changes (primary model swap) are applied immediately during self-healing events.
- **Automation Fix**: Added `export PATH` to `health-check.sh` to ensure cron execution can locate `node` and `openclaw` binaries.

- **Troubleshooting**: Observed auto-reversion from Gemini to Kimi due to `health-check.sh` logic. The script aggressively restores the original primary model upon health check success.
- **Action**: Advised user to disable the health check script or modify `ORIGINAL_PRIMARY` to persist the switch to Gemini.
